<!DOCTYPE html>
<html lang="id">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title><%= title || 'Error' %> - Portal Alumni</title>
        <link href="https://cdn.jsdelivr.net/npm/daisyui@3.9.2/dist/full.css" rel="stylesheet" type="text/css" />
        <script src="https://cdn.tailwindcss.com"></script>
        <style>
            body {
                font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            }
        </style>
        <script>
            function toggleDetails() {
                const details = document.getElementById('errorDetails');
                const toggle = document.getElementById('toggleText');
                if (details.style.display === 'none') {
                    details.style.display = 'block';
                    toggle.textContent = 'Sembunyikan Detail Error';
                } else {
                    details.style.display = 'none';
                    toggle.textContent = 'Tampilkan Detail Error';
                }
            }
        </script>
    </head>
    <body class="min-h-screen flex items-center justify-center bg-[#F0F8F0] text-[#2E7D32]">
        <div class="text-center max-w-2xl p-8">
            <div class="text-5xl mb-4 opacity-70"><% if (typeof status !== 'undefined' && status == 404) { %> 🔍 <% } else { %> ⚠️ <% } %></div>

            <h1 class="text-8xl font-bold text-[#2E7D32] drop-shadow-lg mb-4"><%= typeof status !== 'undefined' ? status : 500 %></h1>

            <p class="text-2xl font-semibold mb-2">
                <% if (typeof status !== 'undefined' && status == 404) { %> Halaman Tidak Ditemukan <% } else { %> <%= typeof message !== 'undefined' ? message : 'Terjadi Kesalahan Internal Server' %> <% } %>
            </p>

            <p class="text-base mb-6 text-[#2E7D32]/80 leading-relaxed">
                <% if (typeof status !== 'undefined' && status == 404) { %> Maaf, halaman yang Anda cari tidak dapat ditemukan di Portal Alumni. Halaman mungkin telah dipindahkan, dihapus, atau URL yang Anda masukkan salah. <% } else { %>
                Maaf, terjadi kesalahan pada server Portal Alumni. Tim teknis kami akan segera memperbaikinya. Silakan coba lagi nanti. <% } %>
            </p>

            <% if ((typeof error !== 'undefined' && error && error.stack) || (typeof stack !== 'undefined' && stack)) { %>
            <div class="cursor-pointer underline text-[#2E7D32]/70 mb-4 hover:text-[#2E7D32] transition-colors" id="toggleText" onclick="toggleDetails()">Tampilkan Detail Error</div>
            <div class="bg-[#E9E7E7] text-[#2E7D32] p-4 rounded-lg mb-6 text-left font-mono text-sm break-all hidden" id="errorDetails" style="display: none">
                <strong>Error Details:</strong><br />
                <pre class="whitespace-pre-wrap mt-2 overflow-x-auto"><%= typeof error !== 'undefined' && error && error.stack ? error.stack : (typeof stack !== 'undefined' ? stack : '') %></pre>
            </div>
            <% } %>

            <div class="space-y-4 sm:space-y-0 sm:space-x-4 sm:flex sm:justify-center">
                <a href="/" class="btn btn-outline rounded-full px-8 py-2 text-[#FFD700] border-[#FFD700] hover:bg-[#FFD700]/20 transition-all duration-300 block sm:inline-block"> Kembali ke Beranda </a>
                <a href="javascript:history.back()" class="btn btn-outline rounded-full px-8 py-2 text-[#FFD700] border-[#FFD700] hover:bg-[#FFD700]/20 transition-all duration-300 block sm:inline-block"> Halaman Sebelumnya </a>
            </div>
        </div>
    </body>
</html>
